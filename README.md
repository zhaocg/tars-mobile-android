# Tars Mobile Android

Native Android client for testing Tars mobile relay chat.

This app mirrors the current iOS mobile client:

- Relay-only connection mode.
- Streaming SSE response handling.
- Message submission through `tars-relay`.
- Initial history rendering starts with the latest 10 messages and loads older
  messages in batches as you scroll upward.
- Markdown rendering in `WebView`.
- Controlled `chart` / `echarts` fenced blocks rendered with bundled ECharts.

## Open in Android Studio

Open this folder in Android Studio and run the `app` configuration on a device
or emulator.

The default Relay mode is `Cloud relay`, using:

```text
https://tarsrelay.pqcenter.cn
```

For local LAN debugging, start `tars-relay/start-local-relay.bat` on the PC and
switch Android Settings to `Local LAN relay`. Use the printed LAN URL as the
Relay URL, for example:

```text
http://192.168.1.20:18992
```

On a physical Android device, do not use `127.0.0.1`; that points to the phone
itself. Use the PC's LAN IP. On the Android emulator, use:

```text
http://10.0.2.2:18992
```

## Relay Settings

In the app Settings dialog:

- Relay Mode:
  - `Cloud relay`: uses `https://tarsrelay.pqcenter.cn`.
  - `Local LAN relay`: connects directly to a relay running on a PC in the same
    LAN; edit Relay URL to the address printed by `start-local-relay.bat`.
  - `Android emulator relay`: uses `http://10.0.2.2:18992`.
  - `Custom relay`: keeps a manually entered Relay URL.
- Relay URL: the relay base URL for the selected mode.
- Session ID: default `mobile-main`.
- Relay Token: the token configured on the relay. New installs do not bundle a
  default token; paste the token generated by local relay or configured on the
  cloud relay.
- Agent ID: must match local Tars `TARS_MOBILE_RELAY_AGENT_ID`.
- Client ID: generated by the app and used by relay routing.

Local Tars must be started with matching mobile relay settings:

```powershell
$env:TARS_MOBILE_RELAY_BASE_URL = 'http://192.168.1.20:18992'
$env:TARS_MOBILE_RELAY_TOKEN = 'same-token-as-relay'
$env:TARS_MOBILE_RELAY_AGENT_ID = 'default'
npm run start
```

## Endpoints Used

- `GET /integrations/mobile/relay/clients/{clientId}/events`
- `POST /integrations/mobile/relay/agents/{agentId}/sessions/{sessionId}/messages`

Messages are submitted as:

```json
{
  "clientId": "android-device",
  "message": "Hello"
}
```

## Troubleshooting

If the app reports HTTP 404, the Relay URL is wrong or the deployed
`tars-relay` does not include the mobile relay endpoints.

If Android reports an SSL handshake failure, first open
`https://tarsrelay.pqcenter.cn/health` in Chrome on the same device. If Chrome
also fails, fix the relay HTTPS certificate chain before debugging the app.

If Android cannot connect to a LAN relay, verify:

- the phone and PC are on the same Wi-Fi/LAN
- Windows Firewall allows inbound TCP `18992`
- Relay URL uses the PC LAN IP, not `127.0.0.1`
- WebChat was started with the same relay URL/token/agent ID

If Android reports `unexpected end of stream` while using a local relay, it is
usually a transient SSE/socket close from the relay or a stale reused
HTTP/1.1 connection. The app sends `Connection: close` for relay requests and
will reconnect the SSE stream automatically; if it repeats continuously, restart
the local relay and WebChat, then confirm both are using the same token.
